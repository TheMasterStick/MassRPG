# Hold

A drop zone, not a real sprite folder — nothing here is loaded by the
game. Upload your PNGs here and they'll get sorted into the correct
`tiles/`, `resources/`, `structures/`, `monsters/` or `player/` folder
(and renamed to match, if needed) as a separate step.
